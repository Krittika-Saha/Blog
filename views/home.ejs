<%- include('partials/header') -%>
    <h1>Home</h1>
    <p> <%= homeStartingContent %> </p>

    <% Posts.forEach(function(post){ %>
        <h1><%=(post['title'])%></h1>
        <p><%=(post['body'])%></p>
        <% if (post['title'] === postParam) { %>
          <% console.log('Match found!') %>
        <% }; %>
  <%});%>

<%- include('partials/footer') -%>
